# Configurações do Servidor
SERVER_PORT=8080
INSTANCE_ID=server-local

# Redis
REDIS_URL=localhost:6379

# PostgreSQL
POSTGRES_URL=postgres://gosocket:gosocket123@localhost:5432/gosocket?sslmode=disable

# Worker
WORKER_ENABLED=false
WORKER_ID=worker-local
BATCH_SIZE=100
BATCH_TIMEOUT=5s
